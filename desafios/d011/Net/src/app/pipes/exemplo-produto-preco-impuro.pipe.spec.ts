import { ExemploProdutoPrecoImpuroPipe } from './exemplo-produto-preco-impuro.pipe';

describe('ExemploProdutoPrecoImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new ExemploProdutoPrecoImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
