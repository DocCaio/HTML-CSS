import { ExemploProdutoPrecoPipe } from './exemplo-produto-preco.pipe';

describe('ExemploProdutoPrecoPipe', () => {
  it('create an instance', () => {
    const pipe = new ExemploProdutoPrecoPipe();
    expect(pipe).toBeTruthy();
  });
});
