import { CalcPotenciaPipe } from './calc-potencia.pipe';

describe('CalcPotenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcPotenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
